{
  "name": "eksamen",
  "version": "1.0.0",
  "description": "Eksamen prosjekt - Bølger&Skvalp",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install-client": "cd client && npm install",
    "install-backend": "cd backend && npm install",
    "install-all": "npm run install-client && npm run install-backend"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^5.2.0"
  }
}

